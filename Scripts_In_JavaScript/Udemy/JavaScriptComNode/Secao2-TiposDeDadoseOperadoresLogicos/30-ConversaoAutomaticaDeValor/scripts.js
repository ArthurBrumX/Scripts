// Empty Values

    // Temos duas palavras reservadas da linguagem que servem parar estes casos;
    // undefined e null;
    // Sempre que se deparar com estas palavras, o JS basicamente quer dizer que
    // os valores nao exitem.

console.log(undefined)
console.log(null)