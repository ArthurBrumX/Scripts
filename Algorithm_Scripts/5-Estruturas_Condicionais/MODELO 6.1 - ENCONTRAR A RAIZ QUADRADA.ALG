algoritmo "modelo 6.1 - Encontrar a raiz quadrada"
var
   A : real
inicio
      A <- int (RaizQ (85)) // para usar somente a parte inteira do resultado
      escreva (A)
      // algoritmo para encontrar a raiz quadrada de um numero
fimalgoritmo